import { OrderWithCounter } from "@opensea/seaport-js/lib/types"
/**
 * Represents the trade parameters of an order with a counter from the OpenSea Seaport JS library.
 */
type TradeParameters = OrderWithCounter["parameters"]

export { TradeParameters }

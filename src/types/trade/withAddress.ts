/**
 * Represents a type that includes an 'address' property of type string.
 */
type WithAddress = {
  /**
   * @property {string} address - a string representing the address.
   */
  address: string
}

export { WithAddress }

/**
 * Represents the arguments needed to add a report to a conversation.
 * @type AddReportToConversationArgs
 */
export type AddReportToConversationArgs = {
  /**
   * @property {string} id - The ID of the report.
   */
  id: string
  /**
   * @property {string} description - The description of the report.
   */
  description: string
}

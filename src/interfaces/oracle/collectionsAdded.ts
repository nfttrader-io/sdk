/**
 * Interface representing the status of whether collections were added.
 * @interface CollectionsAdded
 */
export interface CollectionsAdded {
  /**
   * @property {boolean} added - A property that indicates if the collections is added or not.
   */
  added: boolean
}
